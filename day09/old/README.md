These were partial attempts that didn't work

Overall, the error came down to `|a.x - b.x + 1| * |a.y - b.y + 1|` needing to be `(|a.x - b.x| + 1) * (|a.y - b.y| + 1)`.

Somehow it worked for part 1, but failed during part 2.
